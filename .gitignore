# Hugo build output
/public/
/resources/_gen/

# Hugo cache
/.hugo_build.lock

# Node modules (if using npm for asset processing)
node_modules/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
*~
*.swp
*.swo
.vscode/
.idea/

# Temporary files
*.tmp
*.temp

# Large video files (host externally)
*.MOV
*.mov
# Source clips dropped in the repo root before being copied into static/
/*.mp4
static/*.mp4
static/*.mov
static/*.avi
# Exception: web-optimised clips embedded on the listen page (under GitHub Pages limits)
!static/img_1640.mp4
!static/shore-sound-2026.mp4